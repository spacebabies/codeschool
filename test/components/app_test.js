import { renderComponent } from '../test_helper';
import App from '../../src/components/app';

describe('App' , () => {
    let component;

    // before(() => {
    //     component  = renderComponent(App);
    // });
    //
    // it('renders App', () => {
    //     expect(component).to.exist;
    // });
    //
    // it('has class App', () => {
    //     expect(component.find('.App')).to.have.length(1);
    // });
    //
    // it('renders Toolbar', () => {
    //     expect(component.find('Toolbar')).to.have.length(1);
    // });
    //
    // it('renders Editor', () => {
    //     expect(component.find('Editor')).to.have.length(1);
    // });
});
